[gd_resource type="Resource" script_class="MasteryNodeData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/mastery_node_data.gd" id="1"]

[resource]
script = ExtResource("1")
id = "scheduled_rest"
display_name = "Scheduled Rest"
description = "Automatically rests returning heroes during Repeat Orders and resumes the mission when they are ready."
branch_id = "automation"
tier = 2
gold_cost = 1200
guild_token_cost = 0
prerequisite_ids = Array[String](["repeat_orders"])
effect_value = {}
max_rank = 1
implemented = true
unlock_tags = Array[String](["scheduled_rest"])